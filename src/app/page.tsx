import { Button } from "@/components/ui/button";
import Image from "next/image";

export default function Home() {
  return (
    <div className="w-full bg-muted-500 flex items-center justify-center min-h-screen">
      <Button variant="teritary">Hello world</Button>
    </div>
  );
}
